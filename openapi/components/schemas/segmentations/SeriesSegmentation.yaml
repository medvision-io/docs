type: object
properties:
  '{StudyInstanceUID}':
    type: object
    properties:
      '{SeriesInstanceUID}':
        type: object
        properties:
          '{SOPInstanceUID-1}':
            $ref: './Frame.yaml'
          metadata:
            type: array
            items:
              $ref: '../common/Metadata.yaml'
      metadata:
        type: array
        items:
          $ref: '../common/Metadata.yaml'