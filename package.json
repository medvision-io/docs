{
  "name": "acme-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^1.0.0-beta.69",
    "copyfiles": "^2.4.1",
    "cpy-cli": "^3.1.1",
    "mkdirp": "^1.0.4",
    "redoc-cli": "^0.13.1",
    "rimraf": "^3.0.2"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "prebuild": "npm run test &&rimraf dist && mkdirp dist",
    "build": "npm run generate-yaml && redoc-cli bundle -t template.hbs dist.yaml && cpy 'docs/**/*' '!docs/index.html' dist && cpy 'redoc-static.html' dist --rename=index.html",
    "test": "openapi lint",
    "generate-yaml": "openapi bundle -o dist"
  }
}
